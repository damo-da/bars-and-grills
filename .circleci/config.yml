version: 2.1
orbs:
  node: circleci/node@4.0.0
workflows:
  node-tests:
    jobs:
      - node/test:
          app-dir: ./app
  django-tests:
    jobs:
      - build:
          working_directory: ~/barsandgrills
          docker: circleci/python3.8.4
